import React, { Fragment } from 'react';

const RegistrationPage = (props) => {
    return (
        <Fragment>
            <h1>Registera dig</h1>
        </Fragment>
    );
};

export default RegistrationPage;